<Window x:Class="LibraryManagement.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="260" Width="380"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username"/>
        <TextBox Grid.Row="1" Margin="0,5,0,10"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Text="Password"/>
        <PasswordBox x:Name="Pwd" Grid.Row="3" Margin="0,5,0,10"/>

        <StackPanel Grid.Row="4">
            <!-- MVVM Command yerine Click kullanacağız -->
            <Button Content="Login" Height="35" Click="Login_Click"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
